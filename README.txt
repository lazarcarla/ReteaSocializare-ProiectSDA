Pentru proiectul meu, am avut de implementat TAD-urile 1-4.
1.TAD lista cu elemente generice cu reprezentare pe array
4.TAD dictionar cu elemente generice cu preprezentare inlantuita.

Din punctul meu de vedere mi-a fost mult mai usor sa instantiez pentru fiecare entitati cate o clasa(User.h,
Friend.h, Message.h, Event.h).

Primul Tad este implementat in Lista.h, apoi este apelat in serviceLista.h/.cpp si, in final in userInterface.h/.cpp,
unde este implementat meniul propriu zis.
Al doilea TAD este implementat in Dictionary.h, apoi este apelat in serviceDictionary.h/.cpp, iar in final in UserInterface.h, .cpp

Am ales sa fac un meniu precum acesta deoarece mi se pare mult mai usor de verificat daca TAD-urile functioneaza corect.

Primul TAD l-am folosit pentru a adauga, sterge, modifica si afisa toate
entitatile, adica user, friend, message, event, iar al doilea l-am folosit doar pentru a lucra cu entitatile de tipul User.


